import React from 'react';

const SeparatorBlack = () => <hr style={styles.separator} />;

const styles = {
    separator: {
        border: 'none',
        borderTop: '1px solid #bbbbbb',
        margin: '10px 0',
    },
};

export default SeparatorBlack;
