import { UnorderedList } from '@chakra-ui/react';

const ModalList = (props) => (
    <UnorderedList
        styleType="none"
        m={0}
        p={0}
        {...props}
    />
);

export default ModalList;
