import React from 'react';
import { Divider as NativeDivider } from "react-native-paper";

// Just a line to separate content
const Divider = () => {
    return (
        <NativeDivider />
    );
}

export default Divider;